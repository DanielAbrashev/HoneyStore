server.port=8082
server.context-path=/adminportal
spring.thymeleaf.cache=false

#DATA SOURCE

#Configurations for database connection
spring.datasource.url=jdbc:mysql://localhost:3306/honeystoredatabase
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Username and secret
spring.datasource.username=root
spring.datasource.password=Da4kozawe=

#Keep the connection alive if idle for long time
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

#JPA

#Show or not log for each query
spring.jpa.show-sql=true

#Hibernate auto ddl
spring.jpa.hibernate.ddl-auto=update

#Allows hibernate to generate SQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
